const init=

const detailReducer= (state, action)=> {
    switch(action.type) {
        case 'DETAIL_PRODUCT':{
            return state
        }
        default :
        return state
    }
}

export default detailReducer