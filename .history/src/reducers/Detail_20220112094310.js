
const detailReducer= (state, action)=> {
    switch(action.type) {
        case 'DETAIL_PRODUCT':{
            break
        }
        default :
        return state
    }
}

export default detailReducer