
const cartReducer= (state =[], action)=> {
    switch(action.type) {
        case 'ADD_CART':{
            con
            return action.payload
        }
        default :
        return state
    }
}

export default cartReducer