
import React from 'react';
import PropTypes from 'prop-types';

admin.propTypes = {
    
};

function admin(props) {
    return (
        <div>
            
        </div>
    );
}

export default admin;