
import React from 'react';


function Admin() {
    const user=const state = useSelector(state => state.state)
    return (
        <div>
            <div>admin</div>
        </div>
    );
}

export default Admin;