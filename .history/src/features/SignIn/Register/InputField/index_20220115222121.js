
import React from 'react';
import PropTypes from 'prop-types';

Input.propTypes = {
    
};

function Input(props) {
    return (
        <div>
            
        </div>
    );
}

export default Input;